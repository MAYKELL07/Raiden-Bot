#!/bin/bash
cd /home/container

# Output Current NodeJs Version
node -v ## only really needed to show what version is being used. Should be changed for different applications

# Run the Server
STARTUP="node start"